# Start here next session

Execute `implementation-plan.md` test-first. Preserve the inclusive overdue-plus-today cutoff and the
approved filter-rail layout. Before each changed test's first run, obtain explicit user review.

# Current state

Milestone 07 is active from the shipped LCTrack `0.1.4` baseline. The user selected layout B and
approved both the written compact review-queue design and implementation planning. No production code
or tests have been changed yet.

# Open concerns

New tests will be development evidence rather than independent verification. The visual companion is
still running and must be stopped before the slice ships.
